configuration:
    nova_config:
        'DEFAULT/debug':
            value: True

    package:
        'mc':
            ensure: absent
    exec:
        'something_weired':
            command: '/bin/touch /tmp/weired'
            unless: '/bin/test -f /tmp/weired'
